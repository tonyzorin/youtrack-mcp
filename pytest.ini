[pytest]
markers =
    integration: mark a test as an integration test that requires connection to a real YouTrack instance
    unit: mark a test as a unit test (default if not specified)
    slow: mark a test as slow-running

testpaths = tests

python_files = test_*.py
python_classes = Test*
python_functions = test_* 